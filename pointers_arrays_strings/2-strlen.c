#include "main.h"

/**
 * _strlen -returns the length of a string
 * @str: the string to get the lengh of
 * Return: the lengh of @str
 */
int _strlen(char *s)

{
	int c = o;

	for (; *s != '\0'; s++)
	{
		c++;
	}
	return (c);
}
