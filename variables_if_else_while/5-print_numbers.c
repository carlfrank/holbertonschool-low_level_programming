#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 *main - prints all single digit numbers
 *Return: Always return 0
 */

int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf(n);
	printf('\n');
	return (0);
}
