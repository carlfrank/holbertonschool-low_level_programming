#include "main.h"

/**
 * main - the alphabet, in lowercase
 *
 * Return: Always (Success)
 */

int main(void)
{
char low;
for (low = 'a'; low <= 'z')
{
	_putchar(low);
}
	_putchar('\n');
	return (0);
}
